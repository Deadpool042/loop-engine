# V16.1 — Isolated Workspace and Project Lock

Status: IMPLEMENTED

## Delivered

- public `WorkspaceManager` contract;
- public `ProjectLockManager` contract;
- isolated worker orchestration with deterministic cleanup;
- local atomic filesystem project-lock adapter;
- local isolated workspace adapter;
- real temporary-directory integration tests;
- failure-path tests proving workspace and lock release.

## Acceptance evidence

- two different projects can acquire locks concurrently;
- two attempts for the same project cannot execute concurrently;
- a released project can be acquired again;
- separate attempts receive separate workspace paths;
- workspace directories are physically removed on release;
- orchestration cleans resources when execution throws;
- filesystem operations remain outside the orchestration contract.

## Next lot

V16.2 — lease heartbeat and renewal.
