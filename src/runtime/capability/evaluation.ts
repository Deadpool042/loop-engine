import { freezeRuntimeCapabilityValue } from "./support.js"; import { validateRuntimeCapability } from "./validation.js"; import type { RuntimeCapabilityInput, RuntimeCapabilityResult } from "./types.js"; const ordered = (values: readonly string[]) => [...values].sort(); export const createRuntimeCapability = (input: RuntimeCapabilityInput): RuntimeCapabilityInput => freezeRuntimeCapabilityValue({ ...input, supportedFeatures: ordered(input.supportedFeatures), declaredConstraints: ordered(input.declaredConstraints), compatibilityReferences: ordered(input.compatibilityReferences) }); export function evaluateRuntimeCapability(input: RuntimeCapabilityInput | null): RuntimeCapabilityResult { // Runtime Capability is metadata only and never executes.
 const diagnostics = freezeRuntimeCapabilityValue([...validateRuntimeCapability(input)].sort((left, right) => left.code.localeCompare(right.code))); const capability = input ? createRuntimeCapability(input) : ({} as RuntimeCapabilityInput); return freezeRuntimeCapabilityValue({ capability, diagnostics, valid: diagnostics.length === 0, metadataOnly: true as const }) as RuntimeCapabilityResult; } export const summarizeRuntimeCapability = (result: RuntimeCapabilityResult) => freezeRuntimeCapabilityValue({ id: result.capability.id, version: result.capability.version, valid: result.valid, metadataOnly: true as const });
