import { freezeExecutionBridgeValue } from "./support.js"; import { validateExecutionBridgeInput } from "./validation.js"; import type { ExecutionBridgeInput, ExecutionBridgeResult } from "./types.js";
export const createExecutionBridgeInput = (input: ExecutionBridgeInput): ExecutionBridgeInput => freezeExecutionBridgeValue({ ...input, evidenceReferences: [...input.evidenceReferences].sort(), request: freezeExecutionBridgeValue({ ...input.request }) });
export function evaluateExecutionBridge(input: ExecutionBridgeInput | null): ExecutionBridgeResult { const diagnostics = freezeExecutionBridgeValue([...validateExecutionBridgeInput(input)].sort((a, b) => a.code.localeCompare(b.code))); const normalized = input ? createExecutionBridgeInput(input) : {} as ExecutionBridgeInput; const valid = diagnostics.length === 0; return freezeExecutionBridgeValue({ input: normalized, checks: freezeExecutionBridgeValue([{ id: "non_operational", passed: true, reason: "Execution Bridge never authorizes execution." }]), diagnostics, valid, bridgeReady: valid, executionAllowed: false as const, executionStarted: false as const }) as ExecutionBridgeResult; }
export const buildExecutionBridgeResult = evaluateExecutionBridge; export const summarizeExecutionBridge = (result: ExecutionBridgeResult) => ({ bridgeReady: result.bridgeReady, executionAllowed: false as const, executionStarted: false as const });
