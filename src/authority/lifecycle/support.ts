import type { AuthorityLifecycleContext, AuthorityLifecycleEvent, AuthorityLifecycleMetadata, AuthorityLifecycleRequirement, AuthorityLifecycleSubject } from "./types.js";
export const AUTHORITY_LIFECYCLE_RFC_VERSION = "revocation-expiry-rfc-v13.3" as const;
export function freezeAuthorityLifecycleValue<T>(value: T): T { if (Array.isArray(value)) value.forEach(freezeAuthorityLifecycleValue); else if (value && typeof value === "object" && !Object.isFrozen(value)) Object.values(value as Record<string, unknown>).forEach(freezeAuthorityLifecycleValue); if (value && typeof value === "object") Object.freeze(value); return value; }
export function normalizedEvents(events: readonly AuthorityLifecycleEvent[]): readonly AuthorityLifecycleEvent[] { return freezeAuthorityLifecycleValue([...events].map((event) => freezeAuthorityLifecycleValue({ ...event, evidenceReferences: [...event.evidenceReferences].sort(), review: freezeAuthorityLifecycleValue({ ...event.review, references: [...event.review.references].sort() }) })).sort((a, b) => a.effectiveAt.localeCompare(b.effectiveAt) || a.id.localeCompare(b.id))); }
export function emptySubject(): AuthorityLifecycleSubject { return freezeAuthorityLifecycleValue({ type: "execution_authority", id: "", version: "", authorityId: "", approvalId: "", policyId: "", providerId: "", protocolId: "", mappingId: "", intentId: "" }); }
export function requirement(id: string, pass: boolean, reason: string): AuthorityLifecycleRequirement { return freezeAuthorityLifecycleValue({ id, outcome: pass ? "pass" : "fail", reason, executionStarted: false }); }
export function metadata(context: AuthorityLifecycleContext): AuthorityLifecycleMetadata { return freezeAuthorityLifecycleValue({ ...(context.metadata ?? {}), rfcVersion: AUTHORITY_LIFECYCLE_RFC_VERSION, declarative: true }); }
