import assert from "node:assert/strict"; import { test } from "node:test"; import { AUDIT_RULE_METADATA_COMPLETENESS_RULE, BRIDGE_REQUEST_CONTRACT_RULE, BRIDGE_REQUEST_DIAGNOSTICS_RULE, BRIDGE_REQUEST_DOCUMENT_RULE, BRIDGE_REQUEST_IMMUTABLE_RULE, BRIDGE_REQUEST_MODULE_RULE, BRIDGE_REQUEST_NO_SURFACE_RULE, BRIDGE_REQUEST_PAYLOAD_RULE, BRIDGE_REQUEST_TIME_RULE, BRIDGE_REQUEST_VALIDATION_RULE } from "../../src/audit/rules/audit.js";
test("Bridge Request audit rules are registered and complete", () => { const rules = [BRIDGE_REQUEST_MODULE_RULE, BRIDGE_REQUEST_IMMUTABLE_RULE, BRIDGE_REQUEST_CONTRACT_RULE, BRIDGE_REQUEST_VALIDATION_RULE, BRIDGE_REQUEST_TIME_RULE, BRIDGE_REQUEST_PAYLOAD_RULE, BRIDGE_REQUEST_DIAGNOSTICS_RULE, BRIDGE_REQUEST_DOCUMENT_RULE, BRIDGE_REQUEST_NO_SURFACE_RULE]; assert.deepEqual(rules.map((rule) => rule.id), ["AUDIT-321", "AUDIT-322", "AUDIT-323", "AUDIT-324", "AUDIT-325", "AUDIT-326", "AUDIT-327", "AUDIT-328", "AUDIT-329"]); assert.ok(rules.every((rule) => rule.check().status === "pass")); assert.equal(AUDIT_RULE_METADATA_COMPLETENESS_RULE.check().status, "pass"); });
